# FastAPI Backend Requirements (Lightweight - No Audio Processing)
# This file is for the API server only. Audio processing happens in workers.

# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18

# Database
sqlmodel==0.0.22
psycopg2-binary==2.9.10  # PostgreSQL driver for production
alembic==1.13.1  # Database migrations

# Object Storage
minio==7.2.11  # MinIO Python client for S3-compatible storage

# OpenAI client for Whisper API calls (client only, no audio processing)
openai==1.55.3

# Authentication and Security
passlib[bcrypt]==1.7.4  # Password hashing with bcrypt
PyJWT==2.9.0  # JWT token generation and validation
slowapi==0.1.9  # Rate limiting for FastAPI
bleach==6.0.0  # HTML sanitization to prevent XSS attacks

# Environment and utilities
python-dotenv==1.0.1

# Background Task Processing (client to dispatch tasks)
celery>=5.5.3  # Distributed task queue
redis>=5.0.1  # Redis client for Celery broker/backend

# Logging
python-json-logger==2.0.7  # Structured JSON logging

# AI Actions (LlamaStack)
llama-stack-client
